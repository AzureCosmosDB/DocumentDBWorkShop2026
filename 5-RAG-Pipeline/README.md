# Module 5: RAG Pipeline with Azure DocumentDB

Start with a `before` notebook in Python, C#, or Node.js. The `before` notebooks contain guided `STUDENT EXERCISE` cells with focused TODOs. The matching `after` notebooks contain completed reference solutions.

Participants paste a DocumentDB connection string and OpenAI API key, generate embeddings for chunks, retrieve context with vector and hybrid search, and build a grounded prompt.
